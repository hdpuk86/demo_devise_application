<div class="row">
  <div class="col-lg-6 col-md-10 mt-5 mx-auto">
    <h2>Resend unlock instructions</h2>

    <%= form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post }) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>

      <div class="form-group">
        <%= f.label :email, for: "unlock_with_email" %>

        <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: 'email@example.com', class: "form-control", id: "unlock_with_email" %>
      </div>

      <div class="text-center mb-4">
        <%= f.submit "Resend unlock instructions", class: "btn btn-primary btn-block" %>
      </div>
    <% end %>

    <%= render "devise/shared/links" %>
  </div>
</div>
